# 家系図ノート Ver.1.0 試作4 修正4 親等表示2

内部バージョン: `1.0.0-prototype.4-fix.4-kinship.2`  
IndexedDB: `family-tree-note` / バージョン `4`  
データスキーマ: `4`

家族・親族・先祖の人物情報、関係、出来事、根拠資料を端末内だけで管理するローカルWebアプリです。外部API、CDN、クラウド、アクセス解析は使用せず、人物写真と資料添付もIndexedDBへ保存します。

## 起動方法

### index.htmlを直接開く

[`index.html`](./index.html)をダブルクリックして開きます。通常機能とIndexedDB保存を利用できます。`file://`ではブラウザの制約によりService WorkerとPWAキャッシュは利用しません。

### ローカルHTTPで起動する（推奨）

このフォルダで次を実行し、`http://localhost:8000/`を開きます。

```powershell
python -m http.server 8000
```

HTTP起動ではPWAとオフラインキャッシュを利用できます。GitHub Pagesへ配置する場合も、すべて相対パスのためサブディレクトリで動作します。

初回起動時だけ、表示確認用のサンプル家族を登録します。既に人物データがある場合や、試作1〜3から移行した場合はサンプルを追加・上書きしません。

## Ver.1.0 試作4 修正4 親等表示2

### iPhoneホーム画面アイコン

- `apple-touch-icon.png`を180×180px・不透明PNGで追加しました。Liberty Codex既存6アプリと同じ濃紺背景、左上の淡金色の蝶紋、八角形プレート、アイボリー文字と濃色アウトラインを使用し、中央には「家」だけを表示します。
- 元となる単体SVGは`apple-touch-icon-source.svg`です。外部画像、画像生成AI、Webサービス、外部フォントを使用せず、SVGからPNGを生成しています。
- `index.html`の`apple-touch-icon`は`?v=kinship2`付きの相対パスで参照し、Service WorkerでもPNGをオフラインキャッシュします。既存のmanifest用`icon.svg`は変更していません。

### 人物カードの背景

- 親等表示を「表示しない」にした場合、故人の人物カード本体を薄いグレー`#eeeeee`にします。従来の没年月日と「故人」表示も残ります。
- 親等表示中は、血族・養親族の1〜6親等と、正式に判定された姻族1〜3親等へピンク背景を適用します。1親等`#f6a8bd`、2親等`#f8b8c9`、3親等`#fac7d5`、4親等`#fbd4df`、5親等`#fde1e8`、6親等`#feecf1`の順で薄くなります。
- 背景の優先順位は「1〜6親等のピンク」「故人の薄いグレー」「通常背景」です。基準人物、配偶者・パートナー、元配偶者、継親等、7親等以上、未判定、関係なしは数字の親等背景にしません。
- 親等ごとのCSSクラスだけを人物カードへ付け、幅、高さ、座標、UnionNode、FamilySubtree、CoupleBlock、Direct Lineage Rail、親子線、婚姻二重線、viewBoxは変更しません。通常画面、PNG、印刷用SVGで同じ色を使い、凡例にも6段階と故人色を表示します。

### 関係追加の既存人物候補

- 親、子ども、配偶者・パートナーなどの「関係を追加」で登録済み人物を選ぶ候補行は、写真、イニシャル、よみがな、旧姓、通称、別名、生年月日、故人表示、識別情報を出さず、漢字氏名だけの1行表示にしました。
- 候補の内部値は従来どおりperson IDです。自己関係、重複関係、親子循環、逆向き矛盾、異なる家系図などの除外と保存時検証は変更していません。
- 表示を簡潔にしても検索対象は漢字氏名、よみがな、旧姓、通称、別名のままです。一致度順と、姓・名の漢字を優先する決定的な日本語順も維持します。出来事の人物選択、家族年表、通常の人物一覧など別用途の表示は変更しません。

この更新は表示専用です。IndexedDBはバージョン4のままで、persons、relationships、events、sources、citations、attachments、snapshots、duplicateExclusions、各ID、treeId、sortOrder、親等計算結果を書き換えるマイグレーションはありません。

## Ver.1.0 試作4 修正4 親等表示1

基準人物を起点に、現在の家系図内の人物について1親等から6親等、配偶者、姻族1親等から3親等、継親等、元配偶者、親等外を表示します。人物カード、画面上部の要約、親等凡例、人物詳細の関係経路へ反映し、基準人物を変更すると即座に再計算します。

### 親等計算と関係名

- 生物学上の親子関係は親子エッジ1本を1親等として数えます。傍系は基準人物から共通祖先まで上がり、対象人物まで下るエッジ数の合計です。
- 養親子も親等計算に含めますが、`adoptive`を保持し、血族・養親族・混合経路を区別します。継親子 (`step`) は血族の親等へ含めず、「継父」「継母」「継子」「親等なし」と案内します。
- 配偶者には0親等や1親等を付けず、「夫」「妻」「配偶者」「パートナー」と表示します。性別が未登録なら推測しません。
- 姻族は「基準人物の現在の配偶者の血族」と「基準人物の血族の現在の配偶者」を、配偶者エッジ1回だけの経路として3親等まで判定します。配偶者エッジを2回以上通る経路は自動判定しません。
- `divorced`と`ended`は元配偶者として現在の姻族計算に使用しません。故人の配偶者は「死別した配偶者・姻族関係は要確認」と表示します。
- 父方・母方は最初に経由する親の登録済み性別から確定できる場合だけ表示し、出生年や姓からは推測しません。
- 複数経路は生物学上の血族、養親族、混合経路、姻族、継親等の順を基本に、親等、配偶者エッジ数、不明関係数、関係ID、人物IDで決定的に選択します。
- 子へ下って別の親へ上る経路は、共同親を血族と誤判定するため血族経路に使用しません。不正な循環があっても同じ人物・関係を経路内で再訪せず、探索深度を制限します。

### 人物カード、親等フィルター、関係経路

人物カードは、親等バッジ、写真またはイニシャル、漢字氏名、よみがな・旧姓、具体的な関係名、生没年、状態の順に整理しました。基準人物は文字付きバッジと太い枠で区別し、`aria-label`にも氏名、親等、関係名、故人状態を含めます。カード寸法と座標計算は変更していないため、UnionNode、FamilySubtree、CoupleBlock、Direct Lineage Rail、婚姻二重線の配置は従来どおりです。

表示範囲ダイアログでは「親等と関係名」「親等のみ」「関係名のみ」「表示しない」を選べます。家系図下部の親等強調では1～6親等、配偶者、姻族、親等外を選択でき、対象外の人物と線は削除せず薄く表示します。凡例は折り畳み可能で、390px幅では初期状態を閉じます。

人物詳細の「基準人物からの関係経路」には、親等、関係名、血族・養親族・姻族等の種別、父方・母方、経由人物、経由関係、判定上の注意を表示します。経由人物名からその人物詳細へ移動できますが、基準人物は明示的な「基準人物にする」操作まで変わりません。

親等は表示時に`kinship-calculator.js`で計算する一時データです。`persons`や`relationships`へ書き込まず、IndexedDBバージョンは`4`のままです。settingsへ保存するのは人物カードの親等表示モードだけです。

## Ver.1.0 試作4 修正4 直系配置修正2

### 傍系家族の子ども配置修正

- 直系Railの外側へ親CoupleBlockを移動したとき、そのfamily unitの主系統の子ども・子孫FamilySubtreeも同じX差分だけ追従させます。親だけが移動してchildren-busが長くなる状態を解消し、親UnionNodeと子ども群の中心を真上・真下へそろえます。
- 特定の氏名には依存せず、人物カードが直系Railでロックされていない全family unitへ共通適用します。子どもが複数いる場合はSiblingGroup全体を保ったまま移動し、子どもの配偶者とその下の子孫も同じ家族枝として移動します。
- 直系Railへ接続する人物を含む枝は移動せず、`family-children-not-under-union`診断へ例外理由を残します。人物カード、relationships、sortOrder、IndexedDBデータは変更しません。

### 直系縦軸を横幅圧縮より優先

- 各直系人物と、その人物の親UnionNodeを結ぶ`DirectLineageRail`を描画時に生成します。基準人物の親、父方・母方祖先、さらに上の祖先へ同じ規則を再帰適用し、目標X、Union X、横ずれ、世代、ロック状態、明示的な例外理由を保持します。
- 直系Railへ参加するUnionNode、人物、CoupleBlockをロックし、世代内の詰め直しやFamilySubtreeのcompaction対象から外しました。横幅を縮めるために直系側を動かさず、兄弟、叔父叔母、配偶者側傍系、未接続グループを左右外側へ移動します。
- 各Railの周囲へ`SpineExclusionZone`を作り、直系人物と親UnionNodeの間へ無関係な人物カードや別familyのchildren-busを置きません。避けるための余白が必要な場合は傍系を移動し、座標を圧縮せずSVGの横幅を拡張します。左右余白は120px以上、上下余白は80px以上です。
- 同世代で幅398pxの祖先CoupleBlockが複数あり、直下の親カード中心間隔より必要幅が大きい場合など、人物カード非重複・婚姻30px隣接・固定世代を同時に守ると完全な0px整列が幾何的に成立しないケースは、直系を重ねず安定した最小ずれへ配分し、`exceptionReason`付きで診断します。データは自動修正しません。
- 初期表示と「基準人物へ戻る」は全体fitを行わず、基準人物を読める保存倍率で中央付近へ置きます。「全体表示」は利用者が明示した場合だけ全boundsをfitし、「100%表示」は倍率1へ戻して基準人物を中央表示します。390px画面でも人物座標自体は縮めず、キャンバス内のパン・拡大縮小で閲覧します。
- 直系ずれ診断は0〜8pxを正常、8px超〜16pxをinfo、16px超〜32pxをwarning、32px超をerrorとします。直系CoupleBlock同士の非重複に由来する明示的な幾何制約例外は理由を残し、通常の空き領域によるずれと区別します。
- `window.__familyTreeLayoutState`へ`directLineageRails`、`spineExclusionZones`、`lockedUnionNodeIds`、`lockedPersonIds`、`compactionMoves`、`viewBoxExpansion`、`initialViewportTarget`を公開します。通常時は大量のConsole出力を行いません。
- 通常画面、PNG、印刷・PDFは同じ固定世代・UnionNode・FamilySubtree・SiblingGroup・CoupleBlock配置を使用します。IndexedDBはバージョン4のままで、persons、relationships、events、sources、citations、attachments、snapshots、settings、duplicateExclusionsを変更するマイグレーションはありません。

## Ver.1.0 試作4 修正4

### 関係性入力の既存人物候補

- 親、子ども、配偶者・パートナーなどの関係性入力で「登録済み人物から選択」を開いたとき、候補を漢字の姓、名、旧姓、姓のよみ、名のよみ、生年月日、人物IDの順で決定的に並べます。`Intl.Collator("ja-JP")`を使い、氏名が不完全な人物は氏名がそろった人物より後ろに表示します。
- 検索時は、姓名の完全一致、姓・名・姓名の前方一致、姓名の部分一致、旧姓・よみがな・通称・別名の一致、の順で優先し、同じ一致度では漢字順に並べます。全角・半角スペース、文字幅、ひらがな・カタカナの差を検索用に正規化します。
- 候補行には氏名を大きく表示し、旧姓、よみがな、生年月日、故人表示、同姓同名の識別情報を補足します。出来事の人物選択と家族年表の人物選択にも同じ漢字順を使用します。
- ソート前に候補配列をコピーし、自己関係、重複関係、矛盾する親子方向、親子循環、別家系図の除外を維持します。表示時の一時配列だけを処理し、IndexedDB、人物、関係、ID、表示順は変更しません。

### 婚姻・パートナー表示の改善

- パートナー関係ごとに`CoupleBlock`を動的生成し、現在の関係を最優先で24〜36px（標準30px）の間隔へ隣接配置します。兄弟3人がそれぞれ結婚している場合も、兄弟順を保ちながら各CoupleBlockを一体として並べます。
- 婚姻・パートナー線は、人物カード左右中央の`partner-left-port`／`partner-right-port`だけを使う水平二重線です。二重線の間隔は5pxで、中央のUnionNodeから一重の親子線を下へ伸ばします。婚姻線と親子線のポート・経路は共有しません。
- `current`は実線二重線、`separated`は破線二重線、`divorced`は中央斜線、`ended`は中央の終了記号、`unknown`は薄い二重線で示します。色だけに依存せず、SVGの`title`と`aria-label`にも種類・状態を持たせます。
- 複数パートナーは人物カードを複製せず、関係ごとに独立したCoupleBlockとUnionNodeを生成します。現在の関係を最も近くへ置き、同じ側の追加関係は`secondary-partner-port`へ安定した順序で割り当てます。子どものchildren-busは関係ごとに分離したままです。
- 二重線の2本には同じ`data-relationship-id`と、`data-relation-role="partner-double-line"`、CoupleBlock、UnionNode、人物ID、状態、使用ポートを記録します。透明なタップ領域から2人の氏名、関係種別、状態、開始日、終了日を確認でき、タップ領域はPNG・印刷へ出力しません。
- CoupleBlock、ポート、座標は描画時だけ計算し、`relationships`やIndexedDBへ保存・補正しません。通常表示、PNG、印刷は同じ二重線SVGを使用します。

### 基準人物を中心とする直系スパイン

- 固定世代レイヤーとFamilySubtree方式を維持し、`focusPersonId`のカード中心を`spineX`とする直系スパインを描画時に生成します。計算結果はIndexedDBへ保存しません。
- 基準人物の親UnionNodeを基準人物の真上へ優先配置し、上の祖先世代では対応する子人物の中心へ祖先CoupleBlockを順次そろえます。下の子孫世代は対応UnionNodeの下へSiblingGroup単位で寄せます。
- CoupleBlockを分割せず、現在の婚姻・パートナーの30px隣接、兄弟順、人物カード1枚の制約を維持します。基準人物が兄弟群の中央でなくても、親UnionNodeと基準人物の縦整列を優先します。
- 直系に含まれない兄弟、叔父叔母、配偶者側祖先等のplacement atomは、既存の左右順を保ったままスパイン外側へ配置し、傍系FamilySubtreeがスパインを横切らないようにします。
- この段階の`directSpine.bounds`による自動fitと緩いずれ基準は、直系配置修正2でDirect Lineage Rail、基準人物中心表示、8／16／32pxの診断基準へ置き換えました。

- 修正3の固定世代レイヤーを維持し、横配置を人物カード先行方式から`UnionNode`、`SiblingGroup`、`FamilySubtree`先行方式へ変更しました。世代、座標、UnionNode、FamilySubtreeは描画のたびにメモリ上で計算し、IndexedDBへ保存しません。
- パートナー関係がある親2人は`union:{partner関係ID}`、共同親は`union:parents:{親ID}--{親ID}`、単独親は`union:single:{親ID}`として決定的に生成します。複数パートナーは別UnionNodeとし、人物カードは常に1枚だけです。
- 子どもの主配置用UnionNodeは、実親2人、実親1人、養親2人、養親1人、継親、安定ID順の優先順位で選びます。この順位はレイアウト専用で、関係の重要性や法的優先順位を変更するものではありません。
- 各UnionNodeから子孫側を`FamilySubtree`として構成し、人物カード幅、配偶者間隔、SiblingGroup幅、子FamilySubtree幅、内部余白から必要幅を末端側からメモ化して再帰計算します。循環参照はactive stackで打ち切り、診断へ残します。
- 同じ主UnionNodeの子どもを分割しない原子的な`SiblingGroup`として配置します。FamilySubtreeの専用横境界を確保し、別家族を兄弟の途中へ入れず、配偶者側祖先は婚姻接続点の外側へ展開します。
- 上から下、下から上の2方向でFamilySubtreeブロックの重心を調整し、カードを個別移動せず、ブロック全体を親中心・子ども群中心へ寄せます。全体幅を縮めることより関係の読み取りを優先します。
- 人物カードには親接続、子接続、左右パートナー、養親、継親、補助Union用の専用ポートを計算します。実親・養親・継親の接続点をずらし、主系統へ補助系統を統合しません。
- 親子線は必ずUnionNodeを起点にし、`parent-stem`、その家族専用の`children-bus`、対象子ども専用の`child-stem`として生成します。children-busの範囲はUnion中心とそのUnionに属する子どものポートだけから求め、FamilySubtree境界外へ出しません。
- 各世代間コリドーでUnion routeごとに専用`TrackGroup`と固有trackを割り当てます。別FamilySubtreeのchildren-busは同じtrackを共有せず、避けられない直交交差は従来どおり非接続のcrossing gapで示します。
- SVGには`data-family-subtree-id`、`data-union-node-id`、`data-target-child-id`、`data-corridor-id`、`data-track-group-id`を追加しました。線をタップすると親、子、関係種別を確認できます。透明な操作用線はPNG・印刷SVGから除外します。
- 描画後に、子どもとUnionの所属、専用ポート終点、relationship親ID、FamilySubtree境界、異なるchildren-busの接触、カード重複、兄弟グループ侵入を逆引き検証します。誤接続、境界外bus、人物複製、relationship不一致はerrorです。
- 通常画面、PNG保存、A4横・A3横印刷は同じUnionNode／FamilySubtreeレイアウトを使用します。`window.__familyTreeLayoutState`からUnionNode、FamilySubtree、SiblingGroup、personPorts、corridor、TrackGroup、route、処理時間を確認できます。
- IndexedDBとデータスキーマはバージョン4のままです。persons、relationships、events、sources、citations、attachments、snapshots、settings、duplicateExclusions、ID、`treeId`、`sortOrder`を変更するマイグレーションはありません。

## Ver.1.0 試作4 修正3

- 現在の基準人物を世代`0`とし、登録済みの親子関係から親を`+1`、子を`-1`へ割り当てる固定世代レイヤー方式へ変更しました。世代値は描画のたびに動的計算し、人物や関係へ保存しません。
- 親子制約を最優先し、パートナー関係は同世代へそろえる低優先度の補助制約として扱います。生年月日は世代推定に使わず、同条件での安定した並び順だけに利用します。
- 同じ世代は同じY座標へそろえ、人物カード領域の間に固定の世代間コリドーを設けました。必要なtrack数に応じてコリドーを縦へ広げ、親子線は原則として隣接する世代間だけを通します。
- 同じ親を持つ子どもを`SiblingGroup`として連続配置し、親2人または単独親と子ども群を`FamilyBlock`として扱います。複数回の重心調整で、配偶者を隣へ置きながら親中央を子ども群中央へ近づけます。
- パートナーごと、親IDの組み合わせごと、単独親ごとに決定的な`familyKey`を生成します。実親・養親・継親は別routeとして線種を維持し、複数パートナーの人物カードは複製しません。
- 基準人物を変更したとき、および表示範囲を変更したときは、対象人物だけで世代、SiblingGroup、FamilyBlock、コリドー、track、viewBox、診断を再計算します。
- 基準人物から到達できない人物は「基準人物との世代未確定」エリアへ分離し、そのコンポーネント内だけで親が上、子が下になる相対配置を行います。
- 親子循環、複数経路による世代矛盾、親子世代差、パートナー世代差、family内の世代不一致、兄弟分断、カード重複・侵入を診断します。矛盾があってもデータを自動修正せず、決定的な暫定配置と控えめな警告表示で描画を継続します。
- 同じレイアウトを通常画面、PNG保存、印刷・PDF保存へ使用します。`window.__familyTreeDiagnostics`、`window.__generationDiagnostics`、`window.__familyTreeLayoutState`からテスト用の診断結果を参照できます。詳細なConsole出力は`window.FAMILY_TREE_DEBUG = true`の場合だけです。
- IndexedDBとデータスキーマはバージョン4のままです。persons、relationships、events、sources、citations、attachments、snapshots、settings、duplicateExclusions、ID、`treeId`、`sortOrder`を変更するマイグレーションはありません。

## Ver.1.0 試作4 修正2

- family unitごとに`partner-line`、`parent-stem`、`children-bus`、`child-stem`を線分モデルとして保持し、異なるfamily unitを同じSVG pathへ結合しません。
- 世代間をコリドーとして扱い、横線区間だけでなく、親側・子側の縦線とパートナー線を含む全経路の投影を衝突判定へ使用します。
- 衝突するfamily unitへグローバルtrackを割り当てます。同じtrackを共有できるのは、線分と安全余白が十分離れている家族だけです。
- 必要なtrack数が多い場合はtrack間隔を線幅以下へ縮めず、子世代以降を下へ移動してコリドーを広げます。人物カードの世代内配置と並び順は維持します。
- 同じ人物に実親・養親・継親など複数経路が入る場合はカード上辺の接続位置を分け、縦線同士が重ならないようにします。
- 避けきれない異なるfamily unitの直交交差には、SVG上で背景の切れ目と短い上側線を描き、T字の接続ではないことを示します。PNG保存と印刷にも同じ表現を使います。
- SVGへ`data-family-key`、`data-relation-role`、親・子ID、track、診断状態を付与し、描画後にカード侵入、同方向の線重複、長すぎる経路、密集コリドーを自動診断します。診断用の文字は画面に表示しません。
- 人物カードを選ぶと関係する家族線を、家族線を選ぶとそのfamily unitと構成員を控えめに強調します。家系図のドラッグ操作とは分離しています。
- IndexedDBはバージョン4のままです。persons、relationships、events、sources、citations、attachments、snapshots、settings、duplicateExclusions、ID、`treeId`、`sortOrder`を変更するマイグレーションはありません。

## Ver.1.0 試作4 修正1

- 隣接する別家族の親子横線が同じ高さで連続し、同じ親グループに見える不具合を修正しました。
- パートナー関係にある親2人と共通の子ども、または単独親と子どもを、一意な`familyKey`を持つfamily unitとして生成します。
- SVGではfamily unitごとに独立した`<g>`へ、親側の縦線、子ども専用の横線、各子どもの縦線を描画します。
- 横線区間が重なる・接触する・12px以内に近接するfamily unitは、14px間隔を上限とする別laneへ割り当てます。世代間の空きが狭い場合もカード内へ侵入しない範囲で間隔を調整します。
- 親グループを子どもグループの中央へ安全な範囲で寄せ、カード同士の間隔と既存の並び順を維持します。負のX座標は家系図全体を右へ移動して余白を確保します。
- この修正は`tree-layout.js`とSVG描画処理だけで関係の解釈・座標を改善するもので、IndexedDB、人物、relationships、表示順、バックアップデータは変更しません。

## 試作4で追加した機能

- 複数の家系図を作成、編集、切り替え、複製、アーカイブ、削除
- 人物単体または関連人物を、別の家系図へコピー・移動
- 戸籍、写真、聞き取り、書籍などの資料管理
- JPEG、PNG、WebP、PDFの端末内添付
- 人物、関係、出来事と資料を結ぶ複数引用
- 確認済み、可能性が高い、未確認、情報が食い違っている、の確認状態
- 人物年表と家族年表
- 手動・自動スナップショット、復元、主要操作の取り消し
- GEDCOM 5.5.1相当のUTF-8入出力
- 家系図ごとの統計
- 全体JSONと家系図単体JSON、3種類の復元方法

試作3までの人物CRUD、親族追加、関係編集・解除、表示順、日付精度、重複候補・人物統合、表示範囲、世代ラベル、人物一覧、情報不足チェック、写真圧縮、PNG保存、印刷・PDF保存、PWAは継続しています。

## 画面と基本操作

### 家系図

ヘッダーの家系図名を押すと家系図管理を開きます。家系図カードから切り替え、名称・説明・表紙色の編集、複製、アーカイブ、削除ができます。

削除時は家系図名の再入力が必要です。人物、関係、出来事、資料、引用、添付をまとめて削除するため、削除前に手動扱いのスナップショットを作成します。最後の1件は削除できません。

家系図はドラッグ移動、ホイールまたはボタンで拡大・縮小できます。人物カードを押すと、PCでは右サイドパネル、スマートフォンでは下から詳細シートを開きます。

### 人物の別家系図へのコピー・移動

人物詳細の「別の家系図へ」から操作します。

- 人物だけ、またはつながっている関連人物を含める
- コピー、または移動
- 対象に含まれない相手との関係は作成しない
- コピー時は人物、写真、対象の出来事、資料、引用、添付を新しいIDへ安全に複製する

家系図をまたぐ関係は登録できません。移動元に残す必要がある共有資料は自動削除しません。

### 資料と添付ファイル

「資料」画面で追加・編集・削除・検索を行います。種類、信頼性で絞り込み、更新日、資料名、取得日で並び替えできます。

資料の種類:

- 戸籍・除籍、証明書、写真、聞き取り、手紙、日記
- 墓碑、新聞、書籍、Webサイト、本人・家族の記憶、その他

信頼性は「信頼性が高い」「参考情報」「未確認」「不明」です。整理の補助であり、アプリが事実を自動判定するものではありません。

添付の制限:

- 対応形式: JPEG、PNG、WebP、PDF
- 1ファイル: 20MBまで
- 1資料の合計: 50MBまで
- 10MBを超える選択ファイルには大容量表示
- 画像にはサムネイルを作成
- 「軽量化して保存」では長辺2200pxを超える資料画像を縮小
- PDFはアイコン表示し、元ファイルを開く・端末へ保存できる

人物写真は従来どおり、EXIF向きを可能な範囲で考慮し、縦横比を保ったまま長辺1200px以内へ調整します。既存写真は起動時に変換しません。

### 引用と確認状態

人物詳細の各項目にある資料アイコン、人物年表、関係メニュー、出来事から資料を引用できます。同じ情報へ複数資料を登録できます。

引用には対象項目、頁・箇所、引用文、メモ、確認状態を保存します。引用解除は`citations`だけを削除し、資料や添付は削除しません。資料削除時は引用件数と添付件数を確認し、引用と出来事内の資料参照を安全に解除します。

人物・関係・出来事には次の確認状態を設定できます。

- `confirmed`: 確認済み
- `probable`: 可能性が高い
- `unconfirmed`: 未確認
- `disputed`: 情報が食い違っている

未確認・食い違いは、小さな記号や線種でも示し、色だけには依存しません。

### 人物年表と家族年表

人物詳細の「年表・出典」では、その人物の出来事と年齢を確認できます。「家族年表」では現在の家系図全体を年代順に表示します。

絞り込み:

- 人物
- 出来事の種類
- 年代
- 確認状態
- 出典の有無

出来事の日付は、年月日、年月、年のみ、年頃、不明に対応します。生年月日が分かる範囲で年齢を計算し、精度が年だけの場合は概算表示にします。未来の出来事や没年月日後の出来事は控えめに案内します。

出来事の種類は、出生、死亡、結婚、離婚、養子縁組、居住、学歴、職歴、兵役、移住、病歴、受賞、埋葬、その他です。センシティブな出来事には`isSensitive`を保存できます。

### スナップショットと元に戻す

自動スナップショットを次の直前に作成します。

- 人物統合
- 人物削除
- 関係解除
- 出来事削除
- 資料削除
- JSON復元
- GEDCOM読み込み
- 大量削除・サンプル再登録

家系図削除前と利用者が作成したものは手動スナップショットとして扱います。自動スナップショットは家系図ごとに直近10件を保持し、手動スナップショットは自動削除しません。復元前にも現在状態を自動保存します。

主要操作の直後は画面下部の「元に戻す」を10秒間利用できます。再起動後は「スナップショット」一覧から復元します。

スナップショットに含むもの:

- 家系図、人物、人物写真、関係、表示順
- 出来事、資料メタデータ、引用
- 重複候補除外、家系図固有設定
- 添付ファイルの参照情報

資料添付のBlob本体は容量増加を避けるため複製しません。資料・家系図を削除した後にスナップショットを復元しても、削除済み添付本体は戻りません。完全な保全にはJSONバックアップを使用してください。

### 統計

現在の家系図について、人物数、存命・故人数、世代数、生年範囲、関係数、出典付き人物、未確認情報、写真登録率、生年月日登録率、未接続人物、重複候補、資料数、出来事件数を表示します。カードから関連画面へ移動できます。

## IndexedDB バージョン4

DB名は`family-tree-note`です。

| ストア | 主な用途 | 主なインデックス |
|---|---|---|
| `trees` | 家系図名、説明、基準人物、色、アーカイブ状態 | なし |
| `persons` | 人物、写真、日付精度、名前拡張、確認状態、`treeId` | `familyName`, `givenName`, `updatedAt`, `treeId` |
| `relationships` | 親子・パートナー、状態、表示順、確認状態、`treeId` | `type`, `fromPersonId`, `toPersonId`, `treeId` |
| `settings` | グローバル設定と家系図固有設定 | なし |
| `duplicateExclusions` | 「別人として除外」した重複候補 | `treeId` |
| `events` | 複数人物に紐づく出来事・年表 | `treeId` |
| `sources` | 資料の書誌・取得情報 | `treeId` |
| `citations` | 資料と人物・関係・出来事の結び付け | `treeId`, `sourceId`, `targetKey` |
| `attachments` | 資料の画像・PDF Blobとサムネイル | `treeId`, `sourceId` |
| `snapshots` | 添付本体を除く復元用履歴 | `treeId` |

`settings`のキー:

- `app`: 現在の家系図ID、スキーマ、移行済み状態などのグローバル設定
- `tree:<treeId>`: 基準人物、倍率、表示範囲、世代ラベル、出力設定などの家系図固有設定

### 試作3からのマイグレーション

IndexedDBの`onupgradeneeded`トランザクション内で、次を一度だけ行います。

1. 新ストアと必要なインデックスを作成
2. `家族の家系図`を1件だけ作成
3. 既存`persons`、`relationships`、`duplicateExclusions`へ同じ`treeId`を補完
4. 既存人物・関係へ安全な確認状態初期値を補完
5. 既存`settings`を維持し、グローバル設定と家系図固有設定へ関連付け
6. `migrationV4Complete`とスキーマ4を記録

既存の人物ID、関係ID、写真、日付精度、関係状態、表示順、表示条件、重複候補除外情報は変更しません。移行トランザクションが失敗した場合、IndexedDBがアップグレード全体をロールバックします。DBを削除・初期化する処理はありません。

## JSONバックアップ

### 全体バックアップ

「全体JSONを書き出す」は次を含みます。

- `appName`, `appVersion`, `schemaVersion`, `exportedAt`, `scope`
- `trees`, `persons`, `relationships`, `events`, `sources`, `citations`
- 写真を含む人物情報
- Data URL化した`attachments`
- 家系図ごとの`settings`
- `duplicateExclusions`

スナップショット履歴は通常バックアップへ含めません。

### 家系図単体バックアップ

設定の「この家系図をJSON保存」は現在の家系図と、その人物、関係、出来事、資料、引用、添付、設定だけを書き出します。

### 復元方法

- 新しい家系図として復元: IDを安全に付け替え、既存データを上書きしない
- 同じIDが存在しない場合のみ追加: 元IDを維持し、1件でも衝突する場合は全体を中止
- 現在の家系図を置き換える: 家系図単体バックアップだけを受け付け、事前スナップショット後に置換

復元は人物ID重複、存在しない参照、自己関係、親子循環、引用先、資料参照、出来事の人物参照、添付上限を事前検証します。書き込みは1トランザクションで行い、途中失敗時は中途半端なデータを残しません。

試作1の`data`内包形式と、試作2・試作3のトップレベル形式を読み込めます。`trees`や`treeId`がない旧バックアップは、新しい家系図を作成して不足項目を安全に補完します。

## GEDCOM 5.5.1相当

### 書き出し

設定の「GEDCOM入出力」から、現在の家系図をUTF-8で保存します。書き出し前に対象人物数・関係数と個人情報の注意を表示します。

主な出力タグ:

`HEAD`, `SOUR`, `GEDC`, `CHAR`, `LANG`, `INDI`, `FAM`, `NAME`, `GIVN`, `SURN`, `SEX`, `BIRT`, `DEAT`, `DATE`, `PLAC`, `FAMS`, `FAMC`, `HUSB`, `WIFE`, `CHIL`, `MARR`, `DIV`, `NOTE`, `TITL`, `AUTH`, `PUBL`, `PAGE`, `QUAY`

一部の出来事は`ADOP`, `RESI`, `EDUC`, `OCCU`, `IMMI`, `BURI`, `EVEN`でも出力します。アプリ固有の情報は`NOTE`または`_FTN_`で始まる独自タグへ補足します。

### 読み込み

ファイルを解析して、家系図名、人物数、家族・関係数、出来事件数、警告、未対応タグ、文字コード表記を確認してから、新しい家系図へ取り込みます。既存家系図を直接上書きしません。

自己関係、循環、重複関係、存在しない参照を拒否します。想定外のタグは可能な範囲で警告として数え、処理全体は停止しません。HEADがない、または人物がない壊れたファイルは安全に拒否します。

GEDCOMにはソフトごとの実装差があります。すべての独自タグ、日付表現、同性・複数パートナー表現、資料構造を完全再現することは保証しません。読み込み後に人物・関係・年表を確認してください。

## プライバシーと出力

- 入力データと添付を外部へ送信しません。
- PNGと印刷では、存命人物の日付、写真、氏名を出力時だけ保護できます。
- JSONとGEDCOMには家系情報そのものが含まれます。
- 出力ファイルをSNSや公開サイトへ直接アップロードせず、安全な場所へ保管してください。
- 暗号化バックアップは未実装です。
- ブラウザの「サイトデータ削除」を行うとIndexedDBも削除される場合があります。

## PWA

Service Workerのキャッシュ名は`family-tree-note-v4-fix4-kinship2-icon-ui1`です。activate時に現在のキャッシュ以外の`family-tree-note-`旧キャッシュを削除します。外部リソースを使わず、HTML、CSS、JavaScript、manifest、iPhone用アイコンを含むアイコン資産をキャッシュします。

## ファイル構成

- `index.html`: 全画面、フォーム、ダイアログ
- `styles.css`: レスポンシブ表示、印刷、家系図UI
- `app.js`: 試作1〜3の人物・関係・家系図・出力機能
- `person-candidate-utils.js`: 関係性入力候補の漢字順比較、検索正規化、一致度順ソート
- `kinship-calculator.js`: 1～6親等、養親族、姻族、継親等、関係名、父方・母方、関係経路の読み取り専用計算
- `db.js`: DB作成、v1〜v4マイグレーション、互換処理
- `db-v4.js`: 複数家系図、資料、出来事、履歴、v4バックアップ
- `generation-resolver.js`: 基準人物からの世代制約計算と矛盾診断
- `union-node-builder.js`: UnionNode生成、子どもの所属Union判定、主配置系統の選択
- `family-subtree-layout.js`: FamilySubtree再帰幅、SiblingGroup、横配置、重心調整
- `couple-block-layout.js`: CoupleBlock生成、パートナー優先の世代内横配置、複数パートナーの安定配置
- `layout-validator.js`: SVG経路とrelationshipsの逆引き検証、境界・誤接続診断
- `tree-layout.js`: 固定世代レイヤー、専用ポート、世代間コリドー、Union起点SVG経路
- `gedcom.js`: GEDCOM解析・生成
- `v4-app.js`: 試作4画面と操作
- `manifest.webmanifest`: PWA情報
- `sw.js`: v4オフラインキャッシュ
- `icon.svg`: ローカルアイコン

ビルド工程と外部ライブラリはありません。

## 確認済み項目

- 試作3相当DBからv4へ移行し、人物ID、関係ID、写真、基準人物、重複除外を保持
- 初期家系図が1件だけ作成されること
- 家系図作成、切り替え、複製、アーカイブ、削除・復元
- 家系図間コピーと異なる家系図間の関係拒否
- 資料、JPEG/PDF添付、20MB超過拒否、引用解除
- 年精度・概算の出来事、人物・関係・出来事の確認状態
- 関係解除とスナップショットからの取り消し
- 自動スナップショット10件上限と手動履歴保持
- 日本語GEDCOMのUTF-8往復、外部形式、未対応タグ、壊れたファイル拒否
- 試作1・2・3 JSON復元、新規・追加・置換、重複ID・循環拒否
- 500人物・700関係でSVGと人物一覧500件を表示
- 390px幅と1440px幅、`file://`直接起動、HTTP/PWA、v4旧キャッシュ削除
- JavaScript構文、manifest JSON、未処理例外がないこと
- family unit単位のSVGグループと`data-family-key`、`data-relation-role`、`data-parent-ids`、`data-child-ids`を確認
- 重なる親子横線区間が別laneになり、隣接家族の線が結合して見えないこと
- 神戸 毅・神戸 享子から神戸 聖二、星川 明久・星川 登志子から神戸 亜紀子・星川 敦彦・星川 洋子、神戸 聖二・神戸 亜紀子の各family unitが独立すること
- 神戸 翼を基準に、指定された祖父母世代`+2`、親世代`+1`、同世代`0`、子世代`-1`の全26人物が親子関係どおりに割り当てられること
- 同世代カードのY座標一致、世代の上下順、隣接世代間コリドー、family別SVG属性、重大なroute診断error 0件
- 親2人・子1〜3人、片親、複数パートナー、実親・養親・継親、世代矛盾、未接続親子、10世代のレイアウト
- 実データ相当の神戸・星川・清田・鴻上の7家族を7つのUnionNode／FamilySubtreeとして分離し、children-bus 7本、重大な逆引き診断error 0件を確認
- 各child-stemの対象子ID、UnionNode、親ID、relationship ID、専用ポート終点と、各children-busのFamilySubtree境界内配置を確認
- 500人物・700関係で、世代計算約18ms、UnionNode生成約19ms、FamilySubtree生成約6ms、幅計算約5ms、座標配置約26ms、route生成約108ms、診断約107ms、合計約315ms（ウォームアップ後3回の中央値相当。Nodeテスト環境の一例で、端末性能により変動）
- 基準人物変更、表示範囲変更、世代ラベル、拡大縮小、ドラッグ、family強調、PNG、A4横・A3横印刷
- 上記表示・出力操作の前後でpersons、relationships、events、sources、citations、attachments、duplicateExclusionsのSHA-256が一致すること
- 390px幅と1440px幅、線タップによる親子確認、PNG、A4横・A3横、`file://`、HTTP、PWA、オフライン再起動、fix4以外の旧キャッシュ削除、Console未処理エラー0件
- 基準人物本人、父母・子、祖父母・孫・兄弟姉妹、曾祖父母、叔父叔母、甥姪、いとこ、5親等・6親等、7親等外の判定
- 養父母・養祖父母、継親、配偶者、配偶者の父母・兄弟姉妹、兄弟姉妹の配偶者、元配偶者、配偶者エッジ2回の未判定
- 神戸 翼を基準とする実データ相当で、神戸 聖二・亜紀子1親等、両家祖父母2親等、舞・悠2親等、敦彦・洋子3親等、知里が配偶者、清田家が姻族1～3親等となること
- 500人物・700関係の親等初回計算約36～61ms、同一データのキャッシュ再利用、入力persons・relationshipsの不変（Nodeテスト環境の例。端末性能により変動）
- 親等強調、折り畳み凡例、詳細経路、390px・1440px、PNG、印刷、`file://`、HTTP/PWA、オフラインで親等表示が維持されること

## 現在の制限事項

- 複雑な近親婚、複数の養親経路、世代矛盾では複数の妥当な経路が残る場合があります。代表経路は決定的に1件を選び、候補数をデバッグ情報へ残します。親等表示は法律相談や戸籍上の確定判定を代替しません。

- GEDCOMは5.5.1相当の初期実装で、すべてのソフトとの完全互換ではありません。
- 独自GEDCOMタグや複雑な資料・引用構造を完全には再現できません。
- 添付保存容量は端末・ブラウザ・空き容量に依存します。
- 自動スナップショットは完全バックアップの代替ではありません。
- スナップショットは資料添付のBlob本体を複製しません。
- 人物移動時、移動対象外の人物との関係は引き継ぎません。
- 大規模・複雑な近親関係では、家系図線が交差する場合があります。
- 固定世代は法律上の親等や年齢区分ではなく、基準人物から登録済み親子関係をたどった相対値です。
- 複数の兄弟グループ同士が婚姻でつながる構成では、兄弟順と配偶者隣接を同時に満たせない場合があります。関係の読み取りやすさを優先し、兄弟グループ分断または長い経路をwarningとして残します。
- 人物カードを複製しないため、複数パートナー、実親・養親・継親が同時にある構成では補助routeが長くなる場合があります。別UnionNodeの線へ統合せず、専用trackとcrossing gapで所属を維持します。
- FamilySubtreeの専用領域を優先するため、スマートフォン画面内へ家系図全体を一度に縮小しません。パンと拡大縮小で閲覧してください。
- 世代矛盾は自動修正しません。親子制約を優先した暫定レイヤーと警告表示を確認し、必要な場合だけ利用者が元データを修正してください。
- クラウド同期、暗号化バックアップ、複数人の同時編集は未実装です。
- iOS Safariのプライベートブラウズや保存容量不足ではIndexedDB保存に失敗する場合があります。

## 今後の候補

- パスフレーズ付き暗号化バックアップ
- 添付ファイルを含む完全スナップショットの選択作成
- GEDCOM 7系と追加タグへの対応
- 資料画像のOCR補助と引用箇所の範囲指定
- 大規模家系図の仮想描画・ページ分割
- 家系図間で共有する人物参照
- ローカルLAN内の明示的な共有・共同編集
- 変更履歴の差分表示
